/**
 * データ分析の最新手法 → Zenn（設定）
 *
 * 統計・機械学習の「手法そのもの」を扱う最近の論文を1日1本選び、
 * 本文 PDF を Gemini に読ませて技術寄りの6観点で紹介し、Zenn の記事として書き出す。
 *
 * Zenn はこのアカウントで新規公開が1日1本前後しか進まない（2026-09-14 実測）。
 * 毎日2本以上コミットすると順番待ちが溜まるので、**1日1本**に抑えている。
 */

module.exports = {
  // --- 論文の選び方（OpenAlex）---
  // 2026-09-23 に上位の顔ぶれを見て選んだトピック（いずれも「手法そのもの」を扱う）:
  //   T11871 発展的な統計手法とモデル / T10136 統計的手法と推測 / T10243 統計手法とベイズ推測 /
  //   T10845 因果推論の発展的手法 / T11303 ベイズモデリングと因果推論 /
  //   T12072 機械学習とアルゴリズム / T12535 機械学習とデータ分類
  // 直近2年・OA・学術誌で約3,600件。上位は TabPFN、glmmTMB、並行トレンドの検定、
  // データ漏洩、欠測値の補完、未観測交絡のある因果推論など
  topics: ['T11871', 'T10136', 'T10243', 'T10845', 'T11303', 'T12072', 'T12535'],
  // 「最新」= 直近2年。その中で被引用数の多い順に選ぶ
  // （公開日順にすると被引用数がほぼ0で当たり外れが大きい。2026-09-23 のユーザー判断）
  recentYears: 2,
  perPage: 50,
  maxSearchPages: 20,

  // 応用研究（手法を道具として使っただけの論文）を弾く。トピック分類だけでは混ざる
  relevanceRule:
    '英語で書かれた、データ分析の手法そのものを扱う論文であること。' +
    '統計手法・機械学習の手法・実験計画・因果推論・モデル選択・評価指標・ソフトウェア実装などを、' +
    '提案・改良・比較・検証しているもの。手法を道具として使っただけの応用研究' +
    '（医療・天文・経済などの個別分野の知見が主題のもの）は false。',
  writerRole: 'データ分析と機械学習に詳しい技術ライター',
  reader: 'データ分析や機械学習を仕事で使う技術者・研究者',

  // 記事の観点。key は台帳と本文の組み立てで使うので変えない
  sections: [
    { key: 'what', heading: '(1) 何を解くための手法か',
      guide: 'この論文が扱う問題設定。どんなデータ・どんな場面で困るのか、何を達成しようとしているのかを具体的に。' },
    { key: 'novelty', heading: '(2) 既存のやり方と何が違うのか',
      guide: '既存の手法の何が不足していて、この論文がどこを変えたのか。論文自身が述べている位置づけに基づく。' },
    { key: 'method', heading: '(3) 手法の中身：仕組みと計算の要点',
      guide: '何をどう計算しているのか、既存手法のどこを変えたのかを、式の意味がわかるように言葉で。' +
        '仮定・ハイパーパラメータ・計算量に触れる。記号の羅列にしない。' },
    { key: 'validation', heading: '(4) どう確かめたか：データと比較結果',
      guide: '検証の設計。使ったデータ（規模・種類）、比較した手法、評価指標、主な結果の数値を、本文に書かれている範囲で。' },
    { key: 'implications', heading: '(5) 使いどころと注意点',
      guide: '仕事で使う人にとっての使いどころ。前提が崩れる場面、計算コスト、データ量の目安、実装の入手しやすさなど。' +
        '論文が示していない性能を断定しないこと。「〜の可能性があります」「論文は〜を報告しています」と書き、' +
        '論文の主張と推測を混ぜないこと。' },
    { key: 'nextLead', heading: '(6) 次に読む論文', lead: true,
      guide: '次に読む論文への導入。**60〜150 字**（後ろに付く論文リストと推薦理由と合わせて観点6の分量になる）。' +
        'どの方向に読み進めるとよいかを具体的に書く。中身のない言い回しで字数を埋めない。' +
        '論文名は書かない（リストはコードが付ける）。' }
  ],
  sectionMinChars: 150,
  sectionMaxChars: 300,

  // --- 本文 PDF ---
  pdfMaxBytes: 40 * 1024 * 1024,   // 本文は pdftotext で文字にするので、大きい PDF でも困らない
  pdfTextMinChars: 3000,      // これより短ければ本文が取れていない（画像だけの PDF など）
  pdfTextMaxChars: 150000,

  // --- Gemini ---
  // 混雑（503）や1日の上限（429）で順に切り替える。名前は models.list で確認したもの
  // 先に品質の高いものを試し、全部混んでいたら軽いものに回す。
  // 名前が違えば 404 が返り、次のモデルに移るだけ（実害はない）
  geminiModels: ['gemini-3.6-flash', 'gemini-3.8-flash', 'gemini-3.5-flash', 'gemini-3.7-flash', 'gemini-3.5-flash-lite'],
  // 全モデルが 503（混雑）で落ちることがある（2026-09-23 の初回実行では当時の4モデルすべて）。
  // 混雑は時間をおくと収まるので、一巡して駄目なら間を置いて巡り直す
  geminiRounds: 4,
  geminiRoundWaitMs: 90 * 1000,
  temperature: 0.4,
  maxOutputTokens: 16384,

  // --- 次に読む論文 ---
  referenceLookupMax: 80,
  readingFromReferences: 6,
  readingFromCiting: 4,

  // --- 用語リンク（日本語版 Wikipedia）---
  wikiLinkMax: 8,

  // --- 記事 ---
  articleLead: '統計・機械学習の手法そのものを扱う最近の論文を、6つの観点から紹介します。',
  zenn: {
    user: 'k518',
    slugPrefix: 'datasci-',        // datasci-w1234567890（slug は 12〜50 文字の英小文字・数字・-_）
    titlePrefix: '【論文紹介】',
    emoji: '📈',
    topics: ['論文紹介', 'データ分析', '機械学習', '統計'],
    type: 'idea'
  },

  // --- 置き場所（リポジトリのルートから）---
  paths: {
    articles: 'articles',
    ledger: 'datasci/ledger.json'
  },

  maxTriesPerRun: 8    // 1回の実行で見る候補の数（PDF が取れない・テーマ外を飛ばす）
};
